<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.solo.MemoMapper">
    <insert id="save">
        INSERT INTO memo
        SET title = #{title},
            content = #{content}
    </insert>

    <update id="modify">
        UPDATE memo
        SET title = #{title},
            content= #{content}
        WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE
        FROM memo
        WHERE id = #{id}
    </delete>
    <select id="findAll">
        SELECT id, title, created_at
        FROM memo
        ORDER BY id DESC
    </select>
    <select id="findOne">
        SELECT *
        FROM memo
        WHERE id = #{id}
    </select>
</mapper>